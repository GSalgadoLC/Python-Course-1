# In this module we learned how to use pi
# Open command prompt
# Type pip install
# pip list
# pip install --upgrage
# pip install requests


# We can now call them in python such as

import requests

response = requests.get("https://google.com")
print(response)
